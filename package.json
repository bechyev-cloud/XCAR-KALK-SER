{
  "name": "xcar-server",
  "version": "1.0.0",
  "description": "XCAR — сервер регистрации (логин/пароль/email/телефон), синхронизации, push-уведомлений, подписки и чёрного списка. Без внешних npm-зависимостей — только встроенные модули Node.js.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {}
}
